# Packages

## Purpose

Contains shared contracts and governed libraries.

## Why It Exists

Cross-service integration should happen through stable contracts, not hidden imports or duplicated schemas.

## Architecture Fit

This directory supports API contracts, event contracts, shared observability conventions, validation rules, UI components, SDKs, and Java shared assets.
