# UI Components

## Purpose

Stores future shared React UI components.

## Why It Exists

Common interface elements should remain accessible, consistent, and reusable across web and admin portals.

## Architecture Fit

This package supports the frontend architecture and design-system boundary.
