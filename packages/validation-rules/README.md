# Validation Rules

## Purpose

Stores shared validation schemas and rule documentation.

## Why It Exists

Survey, evidence, data quality, and API validation must be consistent across services.

## Architecture Fit

This package will later support validation reuse without placing business logic in generic shared libraries.
