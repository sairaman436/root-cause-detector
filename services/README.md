# Services

## Purpose

Contains deployable backend, AI, RAG, agent, reporting, and notification services.

## Why It Exists

Runtime services require clear container, ownership, configuration, and deployment boundaries.

## Architecture Fit

This directory implements the approved service topology: Spring Boot core backend plus independently deployable Python AI services.
