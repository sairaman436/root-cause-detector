# RAG Service

## Purpose

Defines the Python service boundary for document ingestion, hybrid retrieval, metadata filtering, citation validation, and future vector search integration.

## Why It Exists

Knowledge retrieval must remain independently governable from prediction and transactional backend workflows.

## Architecture Fit

This service will later own RAG retrieval and citation controls. Milestone 1 provides dependency and ownership scaffolding only.
