# App Router Boundary

## Purpose

Defines the Next.js App Router directory boundary.

## Why It Exists

Next.js tooling requires an `app` or `pages` directory to run lint and build validation. This directory reserves that framework boundary without adding routes or product UI.

## Architecture Fit

Supports the approved Next.js frontend project structure while keeping Milestone 1 free of business features.
