# Helm Charts

## Purpose

Defines the future Helm chart boundary for deployable services.

## Why It Exists

Kubernetes service deployment needs repeatable charts with environment overrides.

## Architecture Fit

This directory supports the approved deployment strategy. Milestone 1 reserves chart locations only.
