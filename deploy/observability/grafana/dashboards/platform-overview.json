{
  "annotations": {
    "list": []
  },
  "description": "Purpose: Tracks production service health, latency, errors, and saturation. Why it exists: Gives SRE teams a single platform readiness dashboard. Architecture fit: Supports Milestone 11 observability and enterprise readiness.",
  "editable": true,
  "panels": [
    {
      "datasource": "Prometheus",
      "fieldConfig": {
        "defaults": {},
        "overrides": []
      },
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 0,
        "y": 0
      },
      "id": 1,
      "targets": [
        {
          "expr": "up",
          "legendFormat": "{{job}}"
        }
      ],
      "title": "Service Availability",
      "type": "timeseries"
    },
    {
      "datasource": "Prometheus",
      "fieldConfig": {
        "defaults": {},
        "overrides": []
      },
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 12,
        "y": 0
      },
      "id": 2,
      "targets": [
        {
          "expr": "sum(rate(http_server_requests_seconds_count[5m])) by (uri)",
          "legendFormat": "{{uri}}"
        }
      ],
      "title": "HTTP Request Rate",
      "type": "timeseries"
    }
  ],
  "schemaVersion": 39,
  "tags": ["airural", "production"],
  "title": "AI Rural Platform Overview",
  "uid": "airural-platform-overview",
  "version": 1
}
